{
  "name": "seed-resource-centre",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "astro": "^5.3.0",
    "wrangler": "^4.98.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12"
  }
}
